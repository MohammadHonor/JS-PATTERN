let row=window.prompt("Enter row")

let col= window.prompt("Enter col");

for(let i=1; i<=row; i++){

    for(let j=1 ; j<=col ; j++){

         document.write("*")
    }
    document.write(`<br/>`)
}